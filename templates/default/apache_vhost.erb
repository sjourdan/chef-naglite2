<VirtualHost *:80>
  ServerAdmin     <%= node[:naglite2][:server_admin] %>
  ServerName      <%= node[:naglite2][:server_name] %>
  DocumentRoot    <%= node[:naglite2][:dir] %>
  CustomLog       /var/log/apache2/naglite2_access.log combined
  ErrorLog        /var/log/apache2/naglite2_error.log

  <Location />
    AuthName "Restricted Access"
    AuthType Basic
    AuthUserFile "<%= node[:naglite2][:htpasswd] %>"
    require valid-user
  </Location>

</VirtualHost>